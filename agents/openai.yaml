interface:
  display_name: "Teamwork"
  short_description: "Plan, review, execute, verify, and final-review complex implementation work"
  default_prompt: "Use $teamwork for explicit orchestration requests to delegate through team-lead, produce a reviewed plan, execute tasks, verify evidence, and complete final review before shipping."
